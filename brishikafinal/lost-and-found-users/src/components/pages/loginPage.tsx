import { LoginForm } from "../forms/loginForm";

const LoginPage = () => {
  return (
    <div className="mx-4 flex min-h-svh flex-col items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
